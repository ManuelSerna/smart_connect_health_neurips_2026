The "tobacco_1m_raw" dir contains only files related to all image file paths, UIDs, and old hierarchical labels. 

Note that these hierarchical labels are **generally** correct, but there is a lot of noise in the labeling, without further post-processing.
